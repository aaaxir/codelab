"""Tidy up the code to make it easier to understand

Use a variable to represent a person's name, and include some whitespace characters at the beginning and end of the name. Make sure you

Print the name once, so the whitespace around the name is displayed.

Then print the name using each of the three stripping functions, lstrip(), rstrip(), and strip()."""


name="\tAamir\n"
print (name)
print (name.lstrip()) ##lstrip
print (name.rstrip()) ##rstrip
print (name.strip()) ##strip